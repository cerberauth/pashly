# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=jcroql
version: 2

project_name: pashly

before:
  hooks:
    - go mod tidy
    - go generate ./...

snapshot:
  version_template: "{{ incpatch .Version }}-next"

gomod:
  proxy: true

report_sizes: true

metadata:
  mod_timestamp: "{{ .CommitTimestamp }}"

builds:
  - id: pashly
    binary: pashly
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
      - arm
      - "386"
    goarm:
      - "7"
    ignore:
      - goos: windows
        goarch: arm64
      - goos: windows
        goarch: arm
      - goos: darwin
        goarch: "386"
      - goos: darwin
        goarch: arm
    mod_timestamp: "{{ .CommitTimestamp }}"
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{ .CommitDate }}

universal_binaries:
  - id: pashly-universal
    ids:
      - pashly
    replace: true

checksum:
  name_template: "checksums.txt"

source:
  enabled: true

archives:
  - name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    formats: ["tar.gz"]
    format_overrides:
      - goos: windows
        formats: ["zip"]
    builds_info:
      group: root
      owner: root
      mtime: "{{ .CommitDate }}"
    files:
      - README.md
      - LICENSE

sboms:
  - artifacts: archive

signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    output: true
    artifacts: checksum
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "${artifact}"
      - --yes

changelog:
  sort: asc
  use: github
  format: "{{ .SHA }}: {{ .Message }}{{ if .Logins }} ({{ .Logins | englishJoin }}){{ end }}"
  groups:
    - title: "New Features"
      regexp: '^.*?feat(\(.+\))??!?:.+$'
      order: 0
    - title: "Bug Fixes"
      regexp: '^.*?(fix|refactor)(\(.+\))??!?:.+$'
      order: 1
    - title: "Other"
      order: 999
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore\(deps\): '
      - '^(build|ci): '
      - Merge pull request
      - Merge branch

milestones:
  - close: true

release:
  name_template: "v{{ .Version }}"

homebrew_casks:
  - name: pashly
    description: &description "Password generation, hashing, and verification CLI for IAM/CIAM migrations."
    license: &license MIT
    homepage: &homepage https://github.com/cerberauth/pashly
    repository:
      owner: &owner cerberauth
      name: homebrew-tap
      branch: &main main
    commit_author: &commit_author
      name: cerberauth-bot
      email: 268418261+cerberauth-bot@users.noreply.github.com

scoops:
  - name: pashly
    description: *description
    homepage: *homepage
    license: *license
    repository:
      owner: *owner
      name: scoop-bucket
      branch: *main
    commit_author: *commit_author

nfpms:
  - file_name_template: "{{ .ConventionalFileName }}"
    package_name: pashly
    vendor: CerberAuth
    homepage: *homepage
    maintainer: Emmanuel Gautier <emmanuel@cerberauth.com>
    description: *description
    license: *license
    bindir: /usr/bin
    section: utils
    formats:
      - apk
      - deb
      - rpm
      - termux.deb
      - archlinux

snapcrafts:
  - name_template: "{{ .ProjectName }}_{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}"
    title: pashly
    publish: true
    summary: *description
    description: "Pashly generates passwords, hashes them with configurable parameters, and verifies passwords against hashes — built to support IAM/CIAM migration work."
    license: *license
    confinement: strict
    grade: stable
    apps:
      pashly:
        plugs:
          - home
    channel_templates:
      - edge
      - beta
      - candidate
      - stable

aurs:
  - name: pashly-bin
    homepage: *homepage
    description: *description
    maintainers:
      - Emmanuel Gautier <emmanuel@cerberauth.com>
    license: *license
    private_key: "{{ .Env.AUR_KEY }}"
    git_url: ssh://aur@aur.archlinux.org/pashly-bin.git
    skip_upload: auto
    package: |-
      install -Dm755 "./pashly" "${pkgdir}/usr/bin/pashly"
      install -Dm644 "./LICENSE" "${pkgdir}/usr/share/licenses/pashly/LICENSE"

winget:
  - name: pashly
    publisher: CerberAuth
    publisher_url: https://www.cerberauth.com
    license: *license
    license_url: https://github.com/cerberauth/pashly/blob/main/LICENSE
    homepage: *homepage
    short_description: *description
    repository:
      owner: *owner
      name: winget-pkgs
      branch: "pashly-{{.Version}}"

chocolateys:
  - name: pashly
    title: Pashly
    owners: CerberAuth
    authors: CerberAuth
    description: *description
    project_url: *homepage
    icon_url: https://github.com/cerberauth.png?size=128
    license_url: https://github.com/cerberauth/pashly/blob/main/LICENSE
    copyright: CerberAuth
    tags: "password hashing security developer cli"
    summary: *description
    release_notes: "https://github.com/cerberauth/pashly/releases/tag/v{{ .Version }}"
    package_source_url: &source https://github.com/cerberauth/pashly/tree/main
    project_source_url: *source
    docs_url: *homepage
    bug_tracker_url: https://github.com/cerberauth/pashly/issues
    api_key: "{{ .Env.CHOCOLATEY_API_KEY }}"
    source_repo: https://push.chocolatey.org/
    skip_publish: false

dockers_v2:
  - id: pashly-dockerhub
    dockerfile: .docker/Dockerfile-goreleaser
    images:
      - "cerberauth/pashly"
    tags:
      - "{{ .Tag }}"
      - "v{{ .Major }}"
      - "v{{ .Major }}.{{ .Minor }}"
      - "latest"
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/arm/v7

  - id: pashly-ghcr
    dockerfile: .docker/Dockerfile-goreleaser
    images:
      - "ghcr.io/cerberauth/pashly"
    tags:
      - "{{ .Tag }}"
      - "v{{ .Major }}"
      - "v{{ .Major }}.{{ .Minor }}"
      - "latest"
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/arm/v7

docker_signs:
  - cmd: cosign
    artifacts: manifests
    output: true
    args:
      - "sign"
      - "${artifact}@${digest}"
      - "--yes"
